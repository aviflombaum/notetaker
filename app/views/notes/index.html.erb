<div class="col-md-10 col-md-offset-1">

  <div class="row">
    <% @notes.each do |note| %>
    <h4><%= note.name %></h4>
    <p> <%= note.content %>
    </p>
    <% end %>
  </div>

  <!-- <div class="row">
    <h3>These notes need references.</h3>
    <% @notes.without_a_reference.each do |note| %>
      <h4> <%#= note.notebook.title %> -  <%= note.name %> </h4>
    <% end %>
  </div> -->
</div>
